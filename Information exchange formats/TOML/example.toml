numbers = [1, 2, 3, 4, 5] # простой массив
[database] # все элементы ниже будут упаковываться в database
server = "192.168.1.1"
ports = [8001, 8001, 8002]
connection_max = 500
enabled = true

[[servers]] # каждый элемент будет упаковываться в отдельную таблицу
ip = "10.0.0.2"
role = "backend"
